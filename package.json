{
  "name": "vanillajs",
  "version": "1.0.0",
  "description": "",
  "author": "Joel Kusasira-Sutton",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch --preserveWatchOutput",
    "serve": "web-dev-server --app-index index.html --node-resolve",
    "start": "concurrently --raw \"npm run watch\"  \"npm run serve -- --open --watch\""
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.22",
    "concurrently": "^6.2.1",
    "typescript": "^4.4.2"
  }
}
